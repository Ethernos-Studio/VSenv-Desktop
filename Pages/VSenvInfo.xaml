<ui:Page x:Class="VSenvDesktop.Pages.VSenvInfo"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
         Title="Vsenv 信息">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部信息区 -->
        <Grid Grid.Row="0" Margin="10,10,10,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Vertical">
                <TextBlock x:Name="TbLocalVer"  FontSize="22" Text="已安装的 VSenv 版本：检测中…"/>
                <TextBlock x:Name="TbRemoteVer" FontSize="18" Text="最新 VSenv 版本 (GitHub)：检测中…"
                           Margin="0,4,0,0"/>
            </StackPanel>

            <Button Grid.Column="1" Content="下载最新版本" Height="50" Width="245"
                    Click="BtnDownload_Click"/>
        </Grid>

        <!-- 操作区 -->
        <Grid Grid.Row="1" Margin="10,10,10,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="自检" Height="40" Width="120"
                    Click="BtnSelfCheck_Click"/>
            <ComboBox Grid.Column="1" x:Name="CmbDebugCmd" Height="40" Width="160"
                      Margin="10,0,0,0" SelectedIndex="0">
                <ComboBoxItem Content="当前环境 token"/>
            </ComboBox>
            <Button Grid.Column="2" Content="运行"  Height="40" Width="120"
                    Margin="10,0,0,0" HorizontalAlignment="Left"
                    Click="BtnRunDebug_Click"/>
        </Grid>

        <!-- 输出日志 -->
        <GroupBox Grid.Row="2" Header="输出日志" Margin="10,10,10,10">
            <Grid>
                <TextBox x:Name="TxtOutput" IsReadOnly="True" FontFamily="Consolas"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"/>
                <ui:ProgressRing x:Name="Ring" HorizontalAlignment="Center" VerticalAlignment="Center"
                                 IsActive="False" Width="40" Height="40"/>
            </Grid>
        </GroupBox>
    </Grid>
</ui:Page>